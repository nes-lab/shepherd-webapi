[project]
# virtual project for a dev-env
name = "Shepherd-WebAPI"
version = "2025.08.2"
description = "Interface between the Testbed Instance and the User "
readme = "README.md"
dependencies = [
    "shepherd-server[dev,test]",
    "shepherd-client[test]",
    # dev
    "pre-commit",
    "bump2version",
    "pyright",
    "ty",
]
requires-python = ">=3.10"

[tool.uv]
package = false

[tool.uv.sources]
shepherd-server = { path = "./shepherd_server"}
shepherd-client = { path = "./shepherd_client"}
